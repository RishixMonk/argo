apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 5
  template:
    metadata:
      labels:
        app: mtya
    spec:
      containers:
      - name: myapp122
        image: nanajanashia/argocd-app:1.2
        ports:
        - containerPort: 8080
